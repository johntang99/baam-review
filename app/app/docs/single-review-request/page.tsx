import { DocRenderer } from "../_doc-renderer";

export const metadata = {
  title: "Single review request — BAAM Review",
};

export default function SingleReviewRequestPage() {
  return (
    <DocRenderer
      title="Single review request"
      filename="SINGLE_REVIEW_REQUEST_SOP.md"
    />
  );
}
