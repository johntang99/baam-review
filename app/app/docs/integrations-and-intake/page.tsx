import { DocRenderer } from "../_doc-renderer";

export const metadata = {
  title: "Connecting client systems (POS / CRM) — BAAM Review",
};

export default function IntegrationsAndIntakePage() {
  return (
    <DocRenderer
      title="Connecting client systems (POS / CRM)"
      filename="INTEGRATIONS_AND_INTAKE_SOP.md"
    />
  );
}
