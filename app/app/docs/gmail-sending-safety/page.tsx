import { DocRenderer } from "../_doc-renderer";

export const metadata = {
  title: "Gmail sending safety — BAAM Review",
};

export default function GmailSendingSafetyPage() {
  return (
    <DocRenderer
      title="Gmail sending safety"
      filename="GMAIL_SENDING_SAFETY_SOP.md"
    />
  );
}
