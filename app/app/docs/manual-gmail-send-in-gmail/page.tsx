import { DocRenderer } from "../_doc-renderer";

export const metadata = {
  title: "Manual Gmail send in Gmail SOP — BAAM Review",
};

export default function ManualGmailSendInGmailPage() {
  return (
    <DocRenderer
      title="Manual Gmail send in Gmail SOP"
      filename="MANUAL_GMAIL_SEND_IN_GMAIL_SOP.md"
    />
  );
}
