import { DocRenderer } from "../_doc-renderer";

export const metadata = {
  title: "Custom sender setup — BAAM Review",
};

export default function CustomSenderSetupPage() {
  return (
    <DocRenderer
      title="Custom sender setup"
      filename="CUSTOM_SENDER_DOMAIN_SETUP.md"
    />
  );
}
