import { DocRenderer } from "../_doc-renderer";

export const metadata = {
  title: "Bulk review requests — BAAM Review",
};

export default function BulkReviewRequestPage() {
  return (
    <DocRenderer
      title="Bulk review requests"
      filename="BULK_REVIEW_REQUEST_SOP.md"
    />
  );
}
